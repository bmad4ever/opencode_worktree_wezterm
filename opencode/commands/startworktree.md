---
description: Create an OpenCode worktree and launch it in a right WezTerm pane
---
!`powershell.exe -NoProfile -ExecutionPolicy Bypass -Command "& ([Environment]::GetFolderPath('UserProfile') + '\.config\opencode\scripts\startworktree.ps1') -Branch '$ARGUMENTS'"`

Report the result above. If it failed, explain the exact next step needed.
